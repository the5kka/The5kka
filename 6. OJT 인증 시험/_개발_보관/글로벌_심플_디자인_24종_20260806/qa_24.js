const {chromium}=require('playwright');const fs=require('fs');const path=require('path');
(async()=>{const root=__dirname,browser=await chromium.launch({headless:true,executablePath:'C:\\Program Files\\Google\\Chrome\\Application\\chrome.exe'}),page=await browser.newPage({viewport:{width:1600,height:900},deviceScaleFactor:1}),source='file:///'+path.join(root,'global_lab.html').replace(/\\/g,'/'),report=[];for(let id=1;id<=24;id++){const errors=[],handler=e=>errors.push(e.message);page.on('pageerror',handler);await page.goto(`${source}?d=${id}`,{waitUntil:'load'});await page.waitForSelector('#app > *',{timeout:10000});await page.evaluate(()=>document.fonts.ready);const issues=await page.evaluate(()=>{const found=[],app=document.querySelector('#app'),ar=app.getBoundingClientRect(),tol=2,visible=el=>{const s=getComputedStyle(el),r=el.getBoundingClientRect();return s.display!=='none'&&s.visibility!=='hidden'&&r.width>0&&r.height>0};document.querySelectorAll('.btn,.panel,.role-card,.door,.talk-option,.flow-node,.lane,.kiosk-card,.zero-band,.paper,.field .control,.role-pills').forEach((el,n)=>{if(!visible(el))return;const r=el.getBoundingClientRect();if(r.left<ar.left-tol||r.top<ar.top-tol||r.right>ar.right+tol||r.bottom>ar.bottom+tol)found.push(`outside:${el.className}:${n}`)});document.querySelectorAll('.paper-stage,.paper-work').forEach((stage,n)=>{const p=stage.querySelector('.paper');if(!p||!visible(p))return;const s=stage.getBoundingClientRect(),r=p.getBoundingClientRect();if(r.left<s.left-tol||r.top<s.top-tol||r.right>s.right+tol||r.bottom>s.bottom+tol)found.push(`paper-outside:${n}`);const ratio=r.width/r.height;if(Math.abs(ratio-210/297)>.02)found.push(`paper-ratio:${ratio.toFixed(3)}`)});document.querySelectorAll('.btn,.control,.role-pill,.choice').forEach((el,n)=>{if(visible(el)&&(el.scrollWidth>el.clientWidth+3||el.scrollHeight>el.clientHeight+3))found.push(`control-overflow:${n}`)});if(document.body.innerText.includes('${'))found.push('template-visible');if(document.querySelectorAll('svg.lucide').length<1)found.push('icons-missing');if(app.scrollWidth>1602||app.scrollHeight>902)found.push(`app-scroll:${app.scrollWidth}x${app.scrollHeight}`);return[...new Set(found)]});page.off('pageerror',handler);report.push({design:id,issues:[...errors.map(x=>`page-error:${x}`),...issues]})}await browser.close();fs.writeFileSync(path.join(root,'qa_report.json'),JSON.stringify(report,null,2));const failed=report.filter(x=>x.issues.length);console.log(JSON.stringify({checked:24,passed:24-failed.length,failed:failed.length,details:failed},null,2));if(failed.length)process.exitCode=2})().catch(e=>{console.error(e);process.exit(1)});
